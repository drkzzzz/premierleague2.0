spring:
  application:
    name: premier-backend
  datasource:
    url: jdbc:mysql://localhost:3306/premier_league_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      mode: never

server:
  port: 8080

jwt:
  secret: miSecretSuperSeguroParaLaPremierLeagueBackend2024
  expiration: 86400000
  refresh-expiration: 604800000

logging:
  level:
    root: INFO
    com.premier.league: DEBUG
